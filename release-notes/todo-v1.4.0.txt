Mandarinku — To-Do List for Next Version (v1.4.0)
====================================================

────────────────────────────────────────────────────
SUGGESTED ADDITIONS (for your consideration)
────────────────────────────────────────────────────

[ ] 4. Weekly Progress Report (Parent)
      - Auto-summary card in Parent Dashboard: XP earned this week,
        scenarios played, tone accuracy %, sentence builder score.
      - Optional local notification reminder on a parent-set schedule
        (e.g. every Sunday evening): "Milton's weekly Mandarin report is ready."

[ ] 5. Daily Learning Goal / Streak Goal
      - Parent sets a daily XP or scenario target for the child
        (e.g. "earn 50 XP per day" or "complete 1 scenario per day").
      - Progress bar on HomeScreen shows today's goal progress.
      - Streak counter already exists — tie it to meeting the daily goal,
        not just launching the app.

[ ] 6. Handwriting Practice Screen
      - Show stroke order animation for a Chinese character (already have
        StrokeOrderSheet as a bottom sheet).
      - Promote it to a full practice tab: child traces the character on
        screen, basic stroke-order scoring.
      - Requires SVG/canvas stroke data per character — could start with
        the 50 most common characters from existing scenarios.

[ ] 7. Listening Comprehension Quiz Mode

      WHERE IT LIVES
      ──────────────
      NOT a new tab. The bottom nav already has 5 tabs (Roleplay, Flashcard,
      Tone Trainer, Sentence Builder, Progress) — adding a 6th would make it
      cramped, especially on small screens. Instead, this becomes Level 6 in
      the existing 5-level mastery system, unlocked after the child earns 3★
      on Level 5 of a scenario.

      Rationale for Level 6 placement:
        - Tone Trainer = single-word tone recognition (simplest audio task)
        - Flashcard LISTENING mode = single-word listen → identify character
        - AUDIO_TO_TRANSLATION quiz = single-word listen → pick meaning
        - Sentence Builder = assemble words into a sentence (reading/order)
        - Level 6 = hear a full sentence → pick the correct meaning
          This is the hardest audio task and logically sits above all of
          the above, making it a natural mastery ceiling.

      HOW IT WORKS
      ────────────
      - TTS automatically speaks the full textChinese of each dialogue line
        from the scenario (same sentences the child already knows from L1–L5).
      - Child sees 4 English/Indonesian choices and picks the correct meaning.
      - No Chinese characters shown — pure listening, no visual cues.
      - A "Replay 🔊" button lets the child hear the sentence again (max 3
        replays per question to keep it a listening test, not a memory test).
      - 5 questions per run: drawn from the scenario's dialogue lines, not
        just the existing quizQuestions — so it uses all the content already
        in the JSON at no extra authoring cost.

      QUESTION GENERATION (no new JSON fields needed)
      ────────────────────────────────────────────────
      - QuizQuestionGenerator adds a generateLevel6Questions() function.
      - For each dialogue step where speaker = CHARACTER and responseType !=
        TEXT_INPUT, create one question:
          questionChinese  = step.textChinese   (spoken by TTS, hidden visually)
          correct option   = step.textEnglish (or textIndonesian)
          3 distractors    = textEnglish lines from other dialogue steps in
                             the same scenario (shuffled), ensuring they are
                             plausibly similar in length/topic.
      - correctAnswerIndex is randomised across 0–3 as per existing rules.

      SCREEN
      ──────
      - Reuses SentenceBuilderQuizScreen layout with minimal changes:
          - Hide the word-tile assembly area.
          - Show a large 🔊 speaker button in its place.
          - TTS auto-plays when the question appears (same as AUDIO_TO_TRANSLATION).
          - Option buttons show English/Indonesian text only (no Chinese).
      - A new route: quiz/{scenarioId}/{rolePlayScore}/6  (level param added)
        OR handled inside the existing SentenceBuilderQuizScreen by checking
        masteryLevel == 6.

      PARENTAL CONTROLS
      ─────────────────
      - No new disable toggle needed — it inherits the scenario's existing
        disable flag from Parent Dashboard.
      - Parent Dashboard milestone rewards: add LEVEL_6_COMPLETIONS to
        MilestoneType for reward tracking.

      BADGES (3 new, consistent with L2–L5 pattern)
      ───────────────────────────────────────────────
        LEVEL_6_DEBUT   🎧  — first scenario passed at Level 6 (3★)
        LEVEL_6_ACE     🦻  — 3 scenarios passed at Level 6 (3★)
        LEVEL_6_MASTER  👂  — all scenarios passed at Level 6 (3★)

[ ] 7b. Level 7 Mastery — Speaking Practice

      FEASIBILITY VERDICT
      ───────────────────
      Possible — but NOT via GBoard. Here is why, and what to do instead.

      WHY GBOARD IS THE WRONG TOOL
      ─────────────────────────────
      GBoard is a separate system app (an IME — Input Method Editor).
      Android provides no public API to:
        - Hide the keyboard layout while keeping only the microphone visible.
        - Force GBoard into voice-only mode programmatically.
        - Intercept or suppress its typing surface from another app.
      Any approach that tries to overlay or manipulate GBoard's window
      would be fragile, break on non-GBoard devices (Samsung, OPPO, etc.),
      and is likely to stop working on future Android versions.

      THE RIGHT TOOL: Android SpeechRecognizer API
      ─────────────────────────────────────────────
      Android ships a built-in SpeechRecognizer (android.speech.SpeechRecognizer)
      that is completely independent of the keyboard. It:
        - Records from the microphone directly.
        - Sends audio to Google's speech-to-text engine (same engine GBoard uses).
        - Returns simplified Chinese characters when locale is set to zh-CN.
        - Gives full programmatic control — no keyboard involved at all.
        - Works on ALL Android devices regardless of which keyboard is installed.
      This is how Duolingo, Google Translate, and every serious language app
      handle in-app speaking. It is the standard approach.

      REQUIRES INTERNET (first use) / OFFLINE MODEL (optional)
      ──────────────────────────────────────────────────────────
      The app already has android.permission.INTERNET declared.
      Speech recognition sends audio to Google's servers by default.
      Google also ships an offline Chinese speech model downloadable via
      Settings → Language & Input → Offline Speech Recognition.
      The app can detect offline availability and show a gentle notice
      if only online recognition is available (similar to the TTS voice
      pack notice in item 10).

      NEW PERMISSION REQUIRED
      ───────────────────────
      AndroidManifest.xml needs:
        <uses-permission android:name="android.permission.RECORD_AUDIO" />
      The app must request this at runtime (Android 6+). A one-time
      permission prompt is shown the first time the child reaches L7.

      WHERE IT LIVES
      ──────────────
      Level 7 in the mastery system — unlocked after 3★ on Level 6.
      The full mastery ladder becomes:
        L1  Standard quiz
        L2  12 targeted vocab questions
        L3  All words in scenario tested
        L4  Sentence Builder (multiple choice)
        L5  Sentence Builder (all sentences, no hints)
        L6  Listening Comprehension (hear sentence → pick meaning)
        L7  Speaking Practice (hear sentence → speak it back)

      HOW IT WORKS — FLOW
      ────────────────────
      1. A dialogue line from the scenario is selected (CHARACTER lines only).
      2. TTS speaks the line at normal speed so the child hears a model.
      3. A large animated 🎤 microphone button is shown.
         Tapping it starts recording (SpeechRecognizer.startListening).
      4. A waveform / pulsing ring animation plays while recording.
         Recording stops automatically when silence is detected
         (SpeechRecognizer handles this) or after a 6-second max.
      5. Recognized text (simplified Chinese characters) is compared to
         the expected textChinese.
      6. Score and feedback are shown (see Scoring below).
      7. 5 questions per L7 run, drawn from the scenario's CHARACTER lines.

      SCREEN LAYOUT (no TextField, no keyboard ever appears)
      ───────────────────────────────────────────────────────
      ┌────────────────────────────────────────┐
      │  [Character emoji + name]              │
      │                                        │
      │  Listen first:                         │
      │  ┌──────────────────────────────────┐  │
      │  │  🔊  "你今天吃了什么？"           │  │  ← tap to replay TTS
      │  │      Nǐ jīntiān chī le shénme?   │  │
      │  └──────────────────────────────────┘  │
      │                                        │
      │  Now you say it:                       │
      │                                        │
      │         [ 🎤  TAP TO SPEAK ]           │  ← large 56dp+ button
      │                                        │
      │  [waveform animation while recording]  │
      │                                        │
      │  [result / feedback card after done]   │
      └────────────────────────────────────────┘

      No text input field is ever shown. No keyboard is ever triggered.
      The mic button is the only interactive element during recording.

      SCORING — CHARACTER MATCH RATE
      ────────────────────────────────
      Compare recognised Chinese characters against expected textChinese
      using a character-level similarity score:

        score = (matching characters / total expected characters) × 100

      Thresholds:
        ≥ 90%  → ✅ Perfect / 很好！   — full marks
        70–89% → 🟡 Close / 再试一次   — half marks, encouraged to retry
        < 70%  → ❌ Try again / 加油！  — no marks, one free retry

      Why character match (not exact string match):
        - SpeechRecognizer may return traditional characters on some engines.
        - Particles (吗, 了, 的) are often dropped in natural speech.
        - An overly strict match would discourage children unfairly.

      ALTERNATIVE SCORING — PINYIN PHONETIC COMPARISON
      ──────────────────────────────────────────────────
      If character match proves too strict (e.g. engine returns wrong
      characters for homophones), fall back to pinyin comparison:
        - Convert expected textChinese → pinyin using the pinyinWords
          field already in the JSON (no extra library needed).
        - Convert recognized Chinese → pinyin using the same mapping.
        - Compare syllable sequences.
      This tolerates character-level errors while still checking pronunciation.
      Can be implemented as a second-pass fallback if the character score
      is below threshold.

      RETRY LOGIC
      ────────────
      - 70–89%: "Almost! Try again 🎤" button offered once.
      - < 70%:  Same retry offer, plus a "Skip this one" option so the
                child is never stuck.
      - After retry, best score of the two attempts is kept.

      BADGES (3 new, consistent with L2–L6 pattern)
      ───────────────────────────────────────────────
        LEVEL_7_DEBUT   🗣️  — first scenario passed at Level 7 (3★)
        LEVEL_7_ACE     🎤  — 3 scenarios passed at Level 7 (3★)
        LEVEL_7_MASTER  🌟  — all scenarios passed at Level 7 (3★)

      MILESTONE REWARD
      ─────────────────
      Add LEVEL_7_COMPLETIONS to MilestoneType.

      PARENTAL CONTROLS
      ─────────────────
      - Inherits the scenario's existing disable flag.
      - No separate toggle needed unless parents request it later.

      IMPLEMENTATION NOTES
      ─────────────────────
      New file: speech/SpeechRecognitionManager.kt
        - Wraps SpeechRecognizer lifecycle (create, startListening,
          stopListening, destroy).
        - Exposes a coroutine-friendly suspendCoroutine wrapper, mirroring
          the existing TtsManager.speakAndAwait() pattern.
        - Handles INSUFFICIENT_PERMISSIONS, ERROR_NO_MATCH, ERROR_NETWORK
          with user-friendly error messages.
      New file: quiz/SpeakingQuizScreen.kt
        - Similar structure to SentenceBuilderQuizScreen but with the
          mic button replacing the word-tile area.
      New route: Routes.SPEAKING_QUIZ ("speaking_quiz/{scenarioId}/{rolePlayScore}")
      QuizQuestionGenerator: add generateLevel7Questions() — selects
        CHARACTER dialogue lines, shuffles, takes up to 5.

[ ] 8. Scenario Bookmarks / Favourites
      - Child (or parent) can star a scenario to pin it to the top of the
        category list or a dedicated "Favourites" section.
      - Useful for scenarios the child loves replaying.

[ ] 9. Character Profile & Avatar
      - Child picks or customises a simple avatar at first launch
        (shown on HomeScreen and Progress tab).
      - Name (already collected in roleplay) displayed on HomeScreen
        as a personalised greeting: "你好, Milton! 🎉"

[ ] 10. Offline TTS Voice Pack Notice
      - Detect if the device has a Chinese TTS voice installed.
      - If not, show a one-time banner with instructions to download
        the Google Chinese (Mandarin) voice for better pronunciation.
      - Many budget Android devices ship without it; this is a common
        parent complaint for language-learning apps.

[ ] 11. Scenario Difficulty Tags
      - Tag each scenario as Beginner / Intermediate / Advanced
        (field in the JSON, shown as a colour badge on scenario cards).
      - Parent Dashboard filter: hide Advanced scenarios until child
        reaches a mastery threshold.

[ ] 12. Export / Backup Progress
      - "Export Progress" button in Parent Dashboard: writes a JSON
        snapshot of all Room data to a file the parent can save.
      - "Restore" option to reload from that file.
      - Safety net before an app reinstall or device change.

────────────────────────────────────────────────────
DEVICE COMPATIBILITY & CROSS-DEVICE UX
(reported issues from real-device testing)
────────────────────────────────────────────────────

[ ] 13. Audio / TTS Not Working on Samsung One UI (reported: Samsung A51)
      DEVICE: Samsung A51 — Android 13, One UI 5.1, Kernel 4.14.113, Knox 3.9

      SYMPTOMS
      ────────
      - TTS audio does not play or plays silently during roleplay, flashcard,
        and tone trainer screens.
      - Likely caused by One UI's battery optimisation aggressively suppressing
        background audio, or the Samsung TTS engine being initialised instead of
        Google TTS, which may not have a Chinese voice pack installed.

      INVESTIGATION CHECKLIST
      ───────────────────────
      1. Detect active TTS engine at runtime (TextToSpeech.getDefaultEngine()) —
         log it in debug builds so we can see whether Samsung TTS or Google TTS
         is being used.
      2. Check TTS initialisation status in TtsManager — if status != SUCCESS,
         currently we silently fail. Show a one-time in-app notice guiding the
         user to install the Google Text-to-Speech engine + Chinese voice pack
         (ties into item 10 — Offline TTS Voice Pack Notice).
      3. Ensure TtsManager requests AudioFocus before speaking
         (AudioManager.requestAudioFocus) and releases it after — required on
         Samsung One UI 5+ to avoid being muted by the OS.
      4. Test against Samsung's Knox audio policy: Knox 3.9 can block certain
         audio streams in "secure mode". Add a fallback that plays audio on
         STREAM_MUSIC instead of STREAM_SPEECH if the latter is blocked.
      5. In AndroidManifest.xml confirm we are not declaring
         android:largeHeap="true" or aggressive doze-exempt flags that Samsung
         One UI penalises with audio throttling.

      DELIVERABLE
      ───────────
      - Robust TTS initialisation with visible error feedback (not silent failure).
      - AudioFocus acquisition/release in TtsManager.speak() and speakAndAwait().
      - One-time banner if Chinese TTS voice is absent (share logic with item 10).

[ ] 15. "Question #/#" Text Cut Off on Quiz Result Screen (reported: Pixel 8 Pro)
      DEVICE: Google Pixel 8 Pro — stock Android, high-resolution display

      SYMPTOMS
      ────────
      - On QuizResultsScreen.kt the label showing "Question 3/5" (or similar
        progress indicator) is clipped or partially hidden, likely at the edge
        of a Card, Column, or Row that does not account for larger system font
        scale settings or the Pixel 8 Pro's display density (3.0x).

      INVESTIGATION CHECKLIST
      ───────────────────────
      1. Locate the "question #/#" composable in QuizResultsScreen.kt and check
         whether its container has a fixed height or maxLines = 1 without
         overflow = TextOverflow.Visible.
      2. Replace any fixed-height containers around text with wrapContentHeight()
         or use Modifier.heightIn(min = ...) instead of Modifier.height(...).
      3. Ensure the text style uses sp units (not dp) so it scales with the
         user's accessibility font size preference — critical for child users
         whose parents may have increased font size.
      4. Add softWrap = true and overflow = TextOverflow.Clip (or Ellipsis with
         a tooltip) as a safety net for extreme font scales.
      5. Test at font scale 1.0×, 1.3×, and 1.5× via Developer Options on a
         high-density screen.

      DELIVERABLE
      ───────────
      - No text clipping on QuizResultsScreen at any system font scale or
        display density. All text containers use flexible sizing.

────────────────────────────────────────────────────
QUIZ RESULT SCREEN & UI POLISH
────────────────────────────────────────────────────

[ ] 18. Apply Active Color Theme to Quiz Result Screen
      SCREEN: QuizResultsScreen.kt

      PROBLEM
      ───────
      The quiz result screen does not fully respond to the user's selected app
      colour theme (theme toggle in HomeScreen / ProgressScreen top bar).
      Background, card surfaces, and text colours appear hardcoded rather than
      using MaterialTheme.appColors semantic tokens.

      WHAT TO DO
      ──────────
      - Audit every Color literal in QuizResultsScreen.kt and replace with the
        appropriate MaterialTheme.appColors or MaterialTheme.colorScheme token.
      - Exceptions (do NOT theme these — they are fixed semantic feedback):
          • Star fill colour (amber — always amber regardless of theme)
          • Correct answer green / wrong answer red on FeedbackCard
      - Ensure the confetti particle colours in ConfettiEffect also pull from
        the active theme palette so they feel cohesive on dark themes.
      - Quick smoke-test: cycle through all 10 themes and confirm the result
        screen looks intentional on each one (no clashing hardcoded colours).

────────────────────────────────────────────────────
FLASHCARD — NEW PRACTICE MODES
────────────────────────────────────────────────────

[ ] 19. Flashcard Speaking Mode
      SCOPE: FlashcardScreen.kt, FlashcardViewModel.kt, PracticeScreen.kt,
             Badges, MilestoneRewards

      BACKGROUND — MODE FLIP BEHAVIOUR
      ──────────────────────────────────
      The flashcard tab currently has two mode buttons: "Listening" and "Reading".
      To avoid adding more buttons and crowding the UI, the new modes use a
      flip-toggle pattern:

        Listening button (active) → tap again → switches to Speaking mode
        Speaking button (active)  → tap again → switches back to Listening mode

        Reading button (active)   → tap again → switches to Writing mode
        Writing button (active)   → tap again → switches back to Reading mode

      Rationale: Listening/Speaking are inverse skills (input vs output for audio);
      Reading/Writing are inverse skills (input vs output for characters). The flip
      makes the relationship intuitive and adds zero extra UI chrome.

      SPEAKING MODE — HOW IT WORKS
      ─────────────────────────────
      - The flashcard shows the Chinese character + pinyin (same as Reading mode
        so the child can see what they are expected to say).
      - A large 🎤 "Tap to Speak" button replaces the "Got it / Skip" controls.
      - Tapping starts SpeechRecognizer (android.speech.SpeechRecognizer, zh-CN)
        — the same engine planned for Level 7 Speaking Practice (item 7b).
      - Recognised text is compared against the card's textChinese using the same
        character-match-rate scoring defined in item 7b:
            ≥ 90%  → ✅ Correct — card is marked mastered for SPEAKING practiceType
            70–89% → 🟡 Close   — show the correct pinyin, offer a retry tap
            < 70%  → ❌ Try again — no mastery credit; card stays in the deck
      - After speaking, the result card shows: recognised text vs expected text,
        with a "Hear correct pronunciation 🔊" replay button (TtsManager).
      - "Skip" option always available so the child is never stuck.

      DATA
      ────
      - Mastery tracked under practiceType = SPEAKING (new enum value in
        PracticeType.kt alongside existing DEFAULT, LISTENING, READING).
      - MasteredWordEntity primary key: (scenarioId, chinese, practiceType) —
        no schema change needed beyond adding the new enum value.
      - Room migration required only if the enum is stored as a string (safe) —
        verify that the existing @TypeConverter handles unknown enum values
        gracefully before adding the new value.

      PERMISSION
      ──────────
      - Requires RECORD_AUDIO permission (same as item 7b).
      - If permission not yet granted, show a friendly one-time prompt when the
        child first taps the Speaking mode button.
      - Reuse SpeechRecognitionManager.kt from item 7b once that is built;
        if item 7b is not yet done, build SpeechRecognitionManager.kt here first.

      BADGES (3 new)
      ──────────────
        SPEAKING_DEBUT   🗣️  — first word mastered in Speaking mode
        SPEAKING_ACE     🎤  — 25 words mastered in Speaking mode
        SPEAKING_MASTER  🌟  — 100 words mastered in Speaking mode

      MILESTONE REWARDS
      ─────────────────
      - Add SPEAKING_WORDS_MASTERED to MilestoneType so parents can set a target
        like "Master 20 words in Speaking mode" as a custom reward criterion.
      - Visible in the milestone reward creation UI in Parent Dashboard alongside
        existing mastery milestone types.

      NOTE: Implement Speaking mode fully and ship it before starting item 20
      (Writing mode). One new mode at a time.

[ ] 20. Flashcard Writing Mode
      SCOPE: FlashcardScreen.kt, FlashcardViewModel.kt, PracticeScreen.kt,
             StrokeOrderSheet.kt (or a new WritingPracticeCanvas.kt),
             Badges, MilestoneRewards

      BACKGROUND
      ──────────
      Writing mode is the flip of Reading mode (see item 19 for the flip-toggle
      pattern). It tests whether the child can correctly reproduce the stroke
      order of a Chinese character by drawing it on screen.

      WRITING MODE — HOW IT WORKS
      ─────────────────────────────
      - The flashcard shows the English / pinyin prompt only — the Chinese
        character is hidden (this is the test).
      - A blank canvas occupies the centre of the card where the child draws
        the character with their finger.
      - A faint grid (米字格, 田字格) overlaid on the canvas helps with proportion,
        matching standard Chinese handwriting practice sheets.
      - Below the canvas: a "Check" button and a "Show Answer" button.
        "Show Answer" reveals the character and its stroke order animation
        (reusing or promoting StrokeOrderSheet logic).

      STROKE-ORDER CHECKING (two-phase approach)
      ────────────────────────────────────────────
      Phase 1 (MVP — self-assessment):
        - After the child draws, the correct character + stroke order animation
          plays over or beside their drawing.
        - Child self-rates: "I got it ✅" or "Not yet ❌".
        - No automated recognition needed — keeps scope small and avoids the
          complexity of handwriting OCR.
        - Mastery credited when child taps "I got it ✅".

      Phase 2 (future enhancement — automated OCR, separate task):
        - Send the canvas bitmap to an on-device ML Kit handwriting model
          (or a lightweight stroke-order recognition library) for automated scoring.
        - Noted here for reference; do NOT implement Phase 2 in this task.

      STROKE ORDER DATA
      ─────────────────
      - StrokeOrderSheet.kt already exists as a bottom sheet.
      - Evaluate whether its stroke data source is sufficient to drive a full
        canvas animation for every character in the scenario pool.
      - If stroke data is missing for a character, fall back gracefully:
        show "Stroke order not available — practice writing and self-check" with
        the static character displayed large.

      DATA
      ────
      - Mastery tracked under practiceType = WRITING (new enum value in
        PracticeType.kt alongside DEFAULT, LISTENING, READING, SPEAKING).
      - Same composite primary key (scenarioId, chinese, practiceType).
      - Room migration: add WRITING to the enum. Verify TypeConverter handles it.

      BADGES (3 new)
      ──────────────
        WRITING_DEBUT   ✍️  — first character mastered in Writing mode
        WRITING_ACE     🖊️  — 25 characters mastered in Writing mode
        WRITING_MASTER  📝  — 100 characters mastered in Writing mode

      MILESTONE REWARDS
      ─────────────────
      - Add WRITING_CHARS_MASTERED to MilestoneType so parents can set a target
        like "Master 20 characters in Writing mode" as a custom reward criterion.
      - Visible in milestone reward creation UI in Parent Dashboard.

      NOTE: Do NOT start this task until item 19 (Speaking mode) is fully
      implemented and tested. One new mode at a time.

[ ] 21. Flashcard Reverse Mode — English → Chinese
      SCOPE: FlashcardScreen.kt, FlashcardViewModel.kt, PracticeScreen.kt,
             PracticeType.kt, Badge.kt, MilestoneReward.kt / MilestoneType

      WHAT IT IS
      ──────────
      The inverse of the existing DEFAULT flashcard mode:

        DEFAULT (current):  card shows Chinese character + pinyin
                            → user picks the correct English meaning from options

        REVERSE (new):      card shows the English word
                            → user picks the correct Chinese character from options

      This completes the bidirectional vocab loop: recognition in both directions.
      Knowing a word in one direction does not guarantee recall in the other —
      English → Chinese is harder and is a natural next challenge once the child
      has mastered the DEFAULT direction.

      UI — WHERE THE BUTTON LIVES
      ────────────────────────────
      Following the same flip-toggle pattern introduced in items 19 & 20:

        DEFAULT button (active) → tap again → flips to REVERSE mode
        REVERSE button (active) → tap again → flips back to DEFAULT mode

      This keeps the mode selector to two visible buttons (DEFAULT and READING)
      with their flip counterparts (REVERSE and WRITING) appearing in-place when
      active, adding zero extra UI chrome.

      HOW IT WORKS
      ─────────────
      - Card face shows: the English word (large, centred).
        Optional: show the Indonesian translation below it in smaller text
        (toggle controlled by the existing "Show Indonesian" user preference).
      - Four answer options displayed as buttons below the card, each showing
        a Chinese character (and its pinyin in small text beneath) — consistent
        with how the DEFAULT mode shows four English options.
      - Distractors: 3 wrong Chinese words drawn from the same scenario's word
        pool, chosen so they are plausibly similar (same character count where
        possible) to make it a genuine test rather than trivial elimination.
      - Correct / wrong feedback: reuse FeedbackCard.kt (same green/red feedback
        as DEFAULT mode).
      - "Got it / Skip" controls replaced by the multiple-choice option buttons,
        mirroring the DEFAULT mode flow exactly (no separate controls needed).

      DATA
      ────
      - Mastery tracked under practiceType = REVERSE (new enum value in
        PracticeType.kt alongside DEFAULT, LISTENING, READING, SPEAKING, WRITING).
      - Composite primary key (scenarioId, chinese, practiceType) — no schema
        change needed beyond adding the new enum value.
      - Verify the existing Room TypeConverter handles the new enum value
        gracefully before adding it (unknown enum → exception risk).
      - No Room migration required if the enum is stored as a String column
        and the TypeConverter uses name-based serialisation.

      BADGES (3 new)
      ──────────────
        REVERSE_DEBUT   🔄  — first word mastered in Reverse mode
        REVERSE_ACE     🔁  — 25 words mastered in Reverse mode
        REVERSE_MASTER  🏆  — 100 words mastered in Reverse mode

      MILESTONE REWARDS
      ─────────────────
      - Add REVERSE_WORDS_MASTERED to MilestoneType so parents can set a target
        like "Master 30 words in Reverse mode" as a custom reward criterion.
      - Visible in the milestone reward creation UI in Parent Dashboard alongside
        all other mastery milestone types.

────────────────────────────────────────────────────
ROLEPLAY MASTERY — SPEAKING LEVELS
────────────────────────────────────────────────────

[ ] 24. Roleplay Quiz Level 7 & Level 8 — Speaking Practice (Word & Sentence)
      SCREENS: new SpeakingWordQuizScreen.kt (L7),
               new SpeakingSentenceQuizScreen.kt (L8)
               (or unified SpeakingQuizScreen.kt with a level parameter)
      RELATED: item 7b (previously planned a single L7 for sentence speaking —
               this item supersedes it by splitting speaking into two levels)

      REVISED MASTERY LADDER
      ───────────────────────
        L1  Standard quiz (multiple choice, Chinese → English)
        L2  12 targeted vocab questions
        L3  All words in scenario tested
        L4  Sentence Builder (multiple choice)
        L5  Sentence Builder (all sentences, no hints)
        L6  Listening Comprehension (hear sentence → pick meaning)
        L7  Speaking — Word Pronunciation (speak each word in the scenario)
        L8  Speaking — Full Sentences (speak every sentence in the scenario)

      NOTE ON ITEM 7b
      ────────────────
      Item 7b described a single L7 for sentence-level speaking. This item
      replaces that plan. When implementing, disregard the L7 content in item 7b
      and follow the two-level design here instead. The technical groundwork in
      item 7b (SpeechRecognitionManager.kt, RECORD_AUDIO permission, scoring
      algorithm) is still valid and should be reused.

      ════════════════════════════════════════════════
      LEVEL 7 — WORD PRONUNCIATION
      ════════════════════════════════════════════════

      WHAT IT TESTS
      ─────────────
      The child must correctly pronounce each individual word (character/token)
      that appears across all dialogue lines in the scenario. Words are drawn
      from the pinyinWords lists already present in the JSON — no new content
      authoring needed.

      QUESTION POOL
      ─────────────
      - Collect every unique Chinese word from all pinyinWords entries in the
        scenario's dialogues (both CHARACTER and STUDENT lines).
      - Deduplicate by textChinese so the same word is not tested twice.
      - Present words one at a time in shuffled order.
      - Total questions = number of unique words in the scenario (varies by
        scenario; typically 15–40 words). Cap at 20 per run if the pool is
        large; remaining words carry over to the next run.

      FLOW PER QUESTION
      ──────────────────
      1. Card shows the Chinese character(s) for the word, large and centred.
         Pinyin is hidden initially (it would give away the pronunciation).
      2. TTS auto-plays the word once at normal speed so the child hears a model.
         A 🔊 replay button lets them hear it again (unlimited replays — this is
         a pronunciation drill, not a listening test).
      3. A large 🎤 "Tap to Speak" button is shown.
         Tapping starts SpeechRecognizer (zh-CN locale).
      4. After recognition:
         - Pinyin is revealed on the card so the child can compare visually.
         - Scored using the character-match-rate algorithm (same as item 7b):
             ≥ 90%  → ✅ Correct  — word counted as pronounced correctly
             70–89% → 🟡 Close    — show pinyin breakdown, offer one retry
             < 70%  → ❌ Try again — show pinyin breakdown, offer one retry
         - After the retry (or if the child skips), advance to next word.
      5. No per-word "try again from the start" — the level-restart option is
         only on the end-of-level results screen (consistent with item 23).

      SCORING
      ───────
      - A word is "passed" if the child scores ≥ 90% on first or second attempt.
      - Star rating at the end of the level:
          3★  ≥ 90% of words passed
          2★  70–89% of words passed
          1★  50–69% of words passed
          0★  < 50% passed (no mastery credit; retry prompted)
      - L8 unlocks when the child earns 3★ on L7.

      SCREEN LAYOUT
      ─────────────
      ┌─────────────────────────────────────────┐
      │  Word 3 of 18           [progress bar]  │
      │                                         │
      │         你                              │  ← large character
      │    (pinyin hidden until after attempt)  │
      │                                         │
      │     [ 🔊 Hear it ]                      │
      │                                         │
      │     [ 🎤  TAP TO SPEAK ]                │  ← 56dp+ button
      │                                         │
      │  [result / pinyin reveal after attempt] │
      └─────────────────────────────────────────┘

      ════════════════════════════════════════════
      LEVEL 8 — FULL SENTENCE PRONUNCIATION
      ════════════════════════════════════════════

      WHAT IT TESTS
      ─────────────
      The child must correctly pronounce every full dialogue line (textChinese)
      from the scenario — both CHARACTER and STUDENT lines. This is the hardest
      speaking task and the mastery ceiling of the entire progression.

      QUESTION POOL
      ─────────────
      - One question per unique dialogue line in the scenario (textChinese).
      - Present in scenario order (not shuffled) — reading a conversation in
        order is more natural and reinforces the story structure.
      - Total questions = number of dialogue steps in the scenario (typically
        8–20 lines). No cap — the child must attempt all lines in one run.

      FLOW PER QUESTION
      ──────────────────
      1. Card shows the full Chinese sentence, large and centred.
         Below it, the English translation is shown in smaller text so the
         child understands what they are about to say.
         Pinyin is hidden initially.
      2. TTS auto-plays the sentence once at normal speed.
         🔊 replay button available (max 3 replays per sentence — this is a
         sentence-level test, so unlimited replay would be too easy).
      3. Large 🎤 "Tap to Speak" button.
         Tapping starts SpeechRecognizer (zh-CN).
      4. After recognition:
         - Full pinyin is revealed beneath the characters.
         - Scored using character-match-rate (same algorithm):
             ≥ 90%  → ✅ Excellent
             70–89% → 🟡 Good — show which characters differed; one retry offered
             < 70%  → ❌ Try again — show full pinyin; one retry offered
         - After retry or skip, advance to next sentence.
      5. No per-sentence restart — end-of-level results screen has "Try Again"
         to restart all sentences from the beginning (consistent with item 23).

      SCORING
      ───────
      - A sentence is "passed" if the child scores ≥ 90% on first or second attempt.
      - Star rating:
          3★  ≥ 90% of sentences passed
          2★  70–89% of sentences passed
          1★  50–69% of sentences passed
          0★  < 50% passed (retry prompted)
      - Completing L8 with 3★ = full scenario mastery (the highest achievable).

      SCREEN LAYOUT
      ─────────────
      ┌─────────────────────────────────────────┐
      │  Sentence 2 of 12       [progress bar]  │
      │                                         │
      │     你今天吃了什么？                     │  ← full sentence, large
      │     What did you eat today?             │  ← translation always visible
      │  (pinyin hidden until after attempt)    │
      │                                         │
      │     [ 🔊 Hear it ]  (max 3 replays)     │
      │                                         │
      │     [ 🎤  TAP TO SPEAK ]                │
      │                                         │
      │  [result / pinyin reveal after attempt] │
      └─────────────────────────────────────────┘

      SHARED TECHNICAL NOTES (L7 & L8)
      ──────────────────────────────────
      - Reuse SpeechRecognitionManager.kt from item 7b / item 19 (build it
        there first if not yet done; do not duplicate the implementation).
      - RECORD_AUDIO permission: request once on first entry to L7; reuse for L8.
      - Both levels use the same SpeechRecognizer lifecycle pattern:
        create on screen entry, destroy on screen exit.
      - For L8, sentences may be long enough that SpeechRecognizer's default
        silence timeout cuts off the child mid-sentence — set
        RecognizerIntent.EXTRA_SPEECH_INPUT_COMPLETE_SILENCE_LENGTH_MILLIS to
        2000 ms (2 s) and EXTRA_SPEECH_INPUT_POSSIBLY_COMPLETE_SILENCE_LENGTH_MILLIS
        to 1500 ms to give more time.
      - Routes:
          Routes.SPEAKING_WORD_QUIZ     "speaking_word_quiz/{scenarioId}/{rolePlayScore}"
          Routes.SPEAKING_SENTENCE_QUIZ "speaking_sentence_quiz/{scenarioId}/{rolePlayScore}"

      BADGES (6 new — 3 per level)
      ──────────────────────────────
      Level 7 — Word Pronunciation:
        L7_WORD_DEBUT   🗣️  — first scenario completed at L7 (3★)
        L7_WORD_ACE     🎙️  — 5 scenarios completed at L7 (3★)
        L7_WORD_MASTER  🥇  — all scenarios completed at L7 (3★)

      Level 8 — Full Sentence:
        L8_SENTENCE_DEBUT   🌟  — first scenario completed at L8 (3★)
        L8_SENTENCE_ACE     🏅  — 5 scenarios completed at L8 (3★)
        L8_SENTENCE_MASTER  🏆  — all scenarios completed at L8 (3★)

      MILESTONE REWARDS
      ─────────────────
      - Add L7_SCENARIO_COMPLETIONS to MilestoneType for parent reward criteria.
      - Add L8_SCENARIO_COMPLETIONS to MilestoneType for parent reward criteria.
      - Both visible in the milestone reward creation UI in Parent Dashboard.

────────────────────────────────────────────────────
VOICE & THEME CONTROLS
────────────────────────────────────────────────────

[ ] 26. In-App TTS Voice Selector
      SCREENS: HomeScreen.kt (TopAppBar), ProgressScreen.kt (TopAppBar)
      RELATED: item 27 (long-press dropdown), UserPreferencesRepository, TtsManager

      WHAT IT DOES
      ────────────
      Lets the user cycle through available Chinese TTS voices installed on
      the device, similar to how the colour-theme button works today.
      A single tap advances to the next voice AND immediately plays a short
      preview phrase (e.g. "你好！" Nǐ hǎo!) in that voice so the user can
      hear the difference.

      WHERE IT LIVES
      ──────────────
      - New icon button placed immediately to the LEFT of the existing
        colour-theme button in the TopAppBar of HomeScreen and ProgressScreen.
      - Icon: Icons.Default.RecordVoiceOver (or similar mic/voice icon).
      - Label below or tooltip: show the voice name or index (e.g. "Voice 2").

      HOW IT WORKS
      ────────────
      1. On first load, enumerate available voices via tts.voices filtered
         to Locale.CHINESE (zh-CN / zh-TW / cmn). Build a stable ordered list.
      2. Store the active voice name in DataStore
         (UserPreferencesRepository — new key: `ttsVoiceName`).
      3. TtsManager.setVoice(name) selects the voice before every speak call.
         Falls back to default locale voice if the stored name is not found.
      4. Tapping the button:
         - Advances to the next voice in the enumerated list (wraps around).
         - Saves the new voice name to DataStore.
         - Immediately calls tts.speak("你好！", rate = 1.0f) as a preview.
      5. If fewer than 2 voices are available for Chinese, hide the button
         (no point showing a selector with nothing to select).

      TECHNICAL NOTES
      ───────────────
      - Voice enumeration must happen after TTS init completes (use the
        existing onInit callback in TtsManager).
      - Expose `availableChineseVoices: List<Voice>` and
        `activeVoiceIndex: Int` as mutableStateOf fields on TtsManager so
        Compose reacts to changes.
      - Preview phrase should use QUEUE_FLUSH so it plays immediately even
        if another utterance is queued.

[ ] 27. Long-Press Dropdown for Voice & Theme Buttons
      SCREENS: HomeScreen.kt (TopAppBar), ProgressScreen.kt (TopAppBar)
      RELATED: item 26 (voice button), existing theme button

      WHAT IT DOES
      ────────────
      When the user long-presses the voice button OR the colour-theme button,
      a dropdown menu appears listing ALL available choices so the user can
      jump directly to any option instead of tapping through them one by one.

      VOICE DROPDOWN (long-press on voice button)
      ───────────────────────────────────────────
      - One DropdownMenuItem per available Chinese voice.
      - Each item shows the voice name and a 🔊 icon.
      - Tapping an item: selects that voice, saves to DataStore, plays preview.
      - Active voice is shown with a checkmark or highlighted row.

      THEME DROPDOWN (long-press on theme button)
      ───────────────────────────────────────────
      - One DropdownMenuItem per AppThemeVariant (emoji + name, e.g. "🌊 Ocean").
      - Tapping an item: applies that theme, saves index to DataStore.
      - Active theme is shown with a checkmark or highlighted row.

      IMPLEMENTATION NOTES
      ─────────────────────
      - Use Compose DropdownMenu anchored to the button's position.
      - Detect long-press via Modifier.pointerInput + detectTapGestures
        { onLongPress = { expanded = true } } on the button Surface/IconButton.
      - Single-tap behaviour remains unchanged (cycle to next, as per item 26
        for voice and existing behaviour for theme).
      - Both dropdowns should dismiss on outside tap or on item selection.

[ ] 28. Revise Onboarding Tour for v1.4 Features
      SCREEN: onboarding/InteractiveOnboardingOverlay.kt
      RELATED: LocalOnboardingCoords, OnboardingKey, UserPreferencesRepository

      WHAT IT COVERS
      ──────────────
      The existing 10-step tour was designed for the original feature set.
      v1.4 adds several new elements that new users should be introduced to:

        1. Voice selector button (item 26) — explain "tap to change voice,
           long-press to pick from a list"
        2. Theme button long-press (item 27) — explain the new dropdown
        3. Sentence Builder tab — now a fully-featured sentence assembly game
           (was absent or minimal in original onboarding)
        4. Mastery level ladder (L1–L8) — brief callout on the scenario card
           showing the level badge, explaining the progression system
        5. Parent Dashboard PIN entry — mention it exists for parents so kids
           know there is a "parent area" (optional — may keep this adults-only)
        6. Indonesian translation toggle — if showIndonesian is exposed in UI,
           show it in onboarding

      APPROACH
      ────────
      - Audit the current 10 steps against the live UI; remove any steps that
        reference UI elements that have been moved or removed in v1.4.
      - Add new OnboardingKey constants for newly tagged elements
        (voice button, sentence builder tab, mastery badge on scenario card).
      - Re-number and reorder steps so the tour flows naturally from top-level
        UI (app bar) → tabs → content area.
      - Keep total step count ≤ 12 to avoid overwhelming new users.
      - Reset onboarding completion flag for existing users on first v1.4 launch
        so they see the updated tour (or offer a "What's new?" replay option
        in the app settings / parent dashboard).

────────────────────────────────────────────────────
COMPLETED
────────────────────────────────────────────────────

────────────────────────────────────────────────────
PARENT DASHBOARD / SECURITY
────────────────────────────────────────────────────

[x] 1. Change PIN
      - Option in Parent Dashboard to change the current 4-digit PIN.
      - Flow: enter current PIN → enter new PIN → confirm new PIN → save.
      - Stored in DataStore (same as current PIN).

[x] 2. Math Password Mode
      - Alternative to PIN: app presents a simple arithmetic challenge
        that a 4–8 year old cannot reliably solve mentally.
      - Examples: "What is 47 + 38?", "What is 9 × 7?", "What is 84 ÷ 4?"
      - Questions generated randomly each time the lock screen is shown
        (so kids can't memorise the answer).
      - Configurable difficulty: Easy (2-digit addition/subtraction),
        Medium (multiplication up to 12×), Hard (division, multi-step).
      - Wrong answer shows a brief cooldown (3 s) before retry, same as PIN.

[x] 3. Password Mode Selector
      - Setting in Parent Dashboard: "Lock Method — PIN / Math Question".
      - Switching modes prompts parent to authenticate with the current
        method first before the change takes effect.
      - PinScreen updated to render either the PIN pad or the math
        challenge depending on the stored mode preference.

────────────────────────────────────────────────────
QUIZ SCREEN UX CLEAN-UP
────────────────────────────────────────────────────

[x] 22. Remove Redundant Feedback Tiles on Quiz Screen (Levels 1–3)
      SCREENS: QuizScreen.kt, FeedbackCard.kt
      AFFECTS: Level 1, Level 2, Level 3 of the roleplay quiz flow

      WHAT TO REMOVE
      ───────────────
      After the user selects an answer, two feedback tiles currently appear:

      1. Wrong answer tile — shows "Not quite" + the correct answer text.
         REDUNDANT because: the correct answer option already turns green
         immediately on selection, so the child can see which one was right
         without a separate tile calling it out.

      2. Correct answer tile — shows "Correct ✅  <Chinese> means <translation>".
         REDUNDANT because: the child just demonstrated they know the meaning
         by choosing correctly — restating the translation adds no value and
         slows down the quiz flow.

      WHAT TO KEEP
      ─────────────
      - The green highlight on the correct option and the red highlight on the
        wrong option — these stay, they are the core visual feedback.
      - The "Next" / advance button that appears after an answer is selected —
        keep the pacing mechanic, just remove the tile above it.
      - Any audio feedback (correct/wrong sound effects) — keep those.

      SCOPE NOTE
      ──────────
      Remove the tiles on Levels 1, 2, and 3 only (the standard quiz levels
      that use QuizScreen.kt / FeedbackCard.kt). Do not change feedback
      behaviour on Sentence Builder (L4/L5), Listening Comprehension (L6), or
      Speaking Practice (L7) — those modes may rely on feedback differently.

[x] 23. Remove Per-Question "Try Again" on Sentence Builder Levels 4 & 5
      SCREENS: SentenceBuilderScreen.kt (or SentenceBuilderQuizScreen.kt)
      AFFECTS: Level 4 and Level 5 of the roleplay mastery progression

      WHAT TO CHANGE
      ───────────────
      Currently, when the user gets a question wrong on L4 or L5, a "Try Again"
      button appears that lets them re-attempt that specific question in place
      before moving on.

      Remove the per-question "Try Again" button entirely. The correct behaviour:
      - Wrong answer → green highlight reveals the correct arrangement (same as
        now), then the flow automatically advances to the next question.
      - User moves forward through all questions regardless of right or wrong.
      - At the end of the level (results/summary screen), a "Try Again" button
        is available to restart the entire level from question 1.

      WHY
      ───
      Per-question retries let the child "grind" a single question until they
      get it right by elimination, which doesn't reflect real learning. Forcing
      a full restart means the child has to hold all the sentences in memory and
      get them all right in one run — a more honest measure of mastery, and
      consistent with how L1–L3 already work.

      WHAT TO KEEP
      ─────────────
      - Green highlight showing the correct sentence order after a wrong answer
        — keep this, it is the learning moment.
      - The brief pause / feedback display after an answer before auto-advancing
        — keep a short delay (≈ 1–1.5 s) so the child can read the correct
        answer before the next question appears.
      - "Try Again" button on the end-of-level results screen — keep this,
        it restarts from question 1.

      SCOPE
      ─────
      L4 and L5 only. Do not change retry behaviour on L1–L3, L6, or L7.

────────────────────────────────────────────────────
AUDIO BUG FIXES
────────────────────────────────────────────────────

[x] 25. Fix Earphone Audio Cut-Off at the Start of Playback
      FILE: tts/TtsManager.kt (primary), possibly MainActivity.kt

      SYMPTOM
      ───────
      When the user has earphones/headphones connected and the app has been
      silent for a moment, the first word — or even the first syllable — of a
      TTS utterance is clipped. The audio then plays normally for the rest of
      the sentence. Affects: roleplay conversation lines, flashcard audio,
      tone trainer, and any other screen that triggers TTS after a period of
      silence.

      ROOT CAUSE
      ──────────
      This is a known Android audio hardware behaviour. When no audio has been
      playing for several seconds, the audio output path (DAC, amplifier, and
      the earphone driver circuit) enters a low-power idle state. When audio
      suddenly starts, the hardware takes 30–150 ms to "wake up" before it
      begins passing signal to the earphone jack or BT codec. The first
      30–150 ms of audio is produced by the app but not yet delivered to the
      earphone, resulting in the clipped-beginning effect.

      The same issue can occur on Bluetooth earphones but is usually more
      pronounced (BT codec reconnection latency can be 100–500 ms).

      SOLUTIONS (in order of preference — try each and keep the one that works
      cleanly without introducing audible artefacts)
      ──────────────────────────────────────────────

      Option A — Silent Audio Primer (recommended first attempt)
        Play a very short (200–300 ms) silent audio clip immediately before
        every TTS utterance. The silence wakes the audio hardware and warms
        the output path without the user hearing anything. The TTS then starts
        into an already-active audio path.

        Implementation:
        - Add a raw resource: res/raw/silence_300ms.mp3 (or .wav) — a 300 ms
          file of pure silence encoded at 44.1 kHz / 16-bit mono.
        - In TtsManager.speak() and speakAndAwait(), play the silence clip via
          MediaPlayer or SoundPool before calling TextToSpeech.speak().
        - For speakAndAwait(), await the silence completion before starting TTS
          so there is no overlap.
        - Silence clip must use the same audio stream as TTS
          (AudioManager.STREAM_MUSIC or the stream TTS is routed to).

      Option B — AudioTrack Keep-Alive
        Create a looping AudioTrack that continuously writes zero-value samples
        (silence) to the audio output at very low volume. This keeps the audio
        hardware permanently active so it is never in the idle state.
        Downside: small continuous battery drain; should only run while the app
        is in the foreground (pause in onStop, resume in onStart).

      Option C — TTS setAudioAttributes with USAGE_MEDIA
        Some OEM TTS engines route audio through a different stream than
        STREAM_MUSIC, which can cause the hardware warm-up issue. Force TTS to
        use AudioAttributes with USAGE_MEDIA and CONTENT_TYPE_SPEECH:
          val attrs = AudioAttributes.Builder()
              .setUsage(AudioAttributes.USAGE_MEDIA)
              .setContentType(AudioAttributes.CONTENT_TYPE_SPEECH)
              .build()
          tts.setAudioAttributes(attrs)
        This ensures TTS and all other app audio share the same hardware path
        and the warm-up from one stream benefits the other.
        Try this first — it is the cheapest fix (one line) and may resolve the
        issue on wired earphones without a silence primer.

      TESTING CHECKLIST
      ─────────────────
      After implementing, verify on:
        □ Wired 3.5 mm earphones — most affected by DAC warm-up latency
        □ Bluetooth earphones (classic A2DP) — most affected by codec reconnect
        □ Bluetooth earphones (aptX / LDAC) — similar to A2DP
        □ Phone speaker (no earphones) — confirm no regression; silence primer
          should be inaudible or absent on speaker path
        □ Samsung One UI device — audio stack differs from stock Android
        □ After a long period of app silence (≥ 10 s) — worst-case scenario
        □ First utterance after app launch — cold-start audio path
────────────────────────────────────────────────────
DEVICE COMPATIBILITY & CROSS-DEVICE UX
────────────────────────────────────────────────────

[x] 14. Bottom Navigation Bar Overflowing into System Nav Bar (reported: OPPO A60)
      DEVICE: OPPO A60 — Android 16, ColorOS, Kernel 5.15.180

      SYMPTOMS
      ────────
      - The app's bottom tab bar (5-tab nav in AppNavigation.kt) renders below
        the system navigation bar area (home / back / recents buttons), making
        the bottom tabs partially or fully untappable on gesture-nav devices.
      - Likely cause: window insets not applied — the app is not consuming
        WindowInsetsCompat.Type.navigationBars() so Compose draws into the
        system-reserved zone.

      DELIVERABLE
      ───────────
      - Edge-to-edge enabled in MainActivity with correct inset handling in
        Scaffold / BottomNavigation so no UI element overlaps system bars on any
        OEM skin or nav mode.

[x] 16. Cross-Device UI Consistency Initiative
      SCOPE: All screens — ensure the app looks and works correctly across
      a range of Android OEM skins, screen sizes, nav modes, and API levels.

      CHECKLIST
      ─────────
      A. Edge-to-Edge & Insets — enableEdgeToEdge() + contentWindowInsets =
         WindowInsets(0) on all Scaffolds; navigationBarsPadding() /
         statusBarsPadding() where needed.
      B. Adaptive Text — TextOverflow.Ellipsis on truncatable labels.
      C. TTS / Audio Resilience — AudioFocus acquire/release + silence primer
         + onChineseVoiceMissing callback wired.
      D. Portrait lock — android:screenOrientation="portrait" in Manifest.
      E–F. API coverage and OEM skin QA — ongoing manual testing.

────────────────────────────────────────────────────
QUIZ RESULT SCREEN & UI POLISH
────────────────────────────────────────────────────

[x] 17. Simplify Quiz Result Screen — Show Correct / Total Only
      SCREEN: QuizResultsScreen.kt

      WHAT WAS DONE
      ─────────────
      - Removed "Total Score" row and "Conversation" summary row.
      - Results card now shows a single line: "You got X / Y correct".
      - Removed Learning Tip tile to eliminate need for scrolling.
      - Fixed results card and question card widths (fillMaxWidth on Box + Column).
